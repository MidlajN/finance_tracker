# Shared Packages

This directory contains platform-independent packages defined in `docs/shared-packages.md`.

- `finance-core` owns deterministic financial business logic.
- `shared-types` owns cross-client contracts.
- `shared-api` owns API and synchronization contracts.
- `shared-utils` owns pure reusable utilities.
- `parser` owns external input normalization and import/export serialization helpers.
